https://flutter.dev/docs/get-started/install/linux
https://www.youtube.com/watch?v=ixj0MMusDM8

In lib

flutter create proj_name
flutter emulators
flutter devices
flutter build linux

flutter run -d linux
  to run on browser

flutter run
  to run on browser

flutter emulators --launch Pixel_2_API_30
flutter emulators --launch Pixel_3a_API_33_x86_64
flutter run
  to run on emulator, both commands


PRESS P IN THE TERMINAL/CONSOLE, THE FRAMES IN THE SIMULATOR WILL APPEAR!
PRESS R  IN THE TERMINAL/CONSOLE TO RELOAD THE APP WITHOUT RESTARTING EVERYTHING

  const HelloWorldPlus(this.number, {this.color = Colors.red, Key? key})
      : super(key: key);

In the curly brackets we have optional fields

Change pubspec.yaml, execute
  flutter pub get

flutter pub add http
  To add to http in yaml, external

Testing:
  dart pub add --dev test
  flutter test test/unit_test.dart

VScode autocompletion:
  CTRL+SHIFT+P then paste:
    Dart Data Class Generator

Firebase:
    dart pub global activate flutterfire_cli
  Add C:\Users\kirby\AppData\Local\Pub\Cache\bin to path
    Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass
    npm install -g firebase-tools
    firebase login
  Nella directory del progetto:
    flutterfire configure --project=dima-app-372615
  Aggiungere le seguenti dependecy, CTRL+shift+P, type dependecy and paste:
    firebase_core, firebase_storage, cloud_firestore, firebase_auth, google_sign_in, flutter_riverpod
  To test it, make main async and add:
    import 'package:firebase_core/firebase_core.dart';
    import 'firebase_options.dart';

    // ...

    await Firebase.initializeApp(
        options: DefaultFirebaseOptions.currentPlatform,
    );
  In pub.dev see google_sign_in documentation
  In web firebase console go to project setting, scroll down to add fingerprint
  We need sha key, so we use:
    https://stackoverflow.com/questions/51845559/generate-sha-1-for-flutter-react-native-android-native-app
    keytool -list -v -keystore ~/.android/debug.keystore -alias androiddebugkey -storepass android -keypass android
  The google-services.json must then be put in andriod/app/src
  If warnings, solve theme (usually in android/app/build.gradle androidsdk to 19 and enable multidex in android: multiDexEnable true, compileSdkVersion 33)
  Add firebase_core_desktop if running on linux
  In build authentication set up google oauth
  In users tab this is where data will be stored